
import 'package:flutter/material.dart';

class LatterColor {

  /// 主色（Primary）：深蓝
  static const Color primary = Color(0xFF2962FF);

  /// 次主色（Secondary）：靛蓝
  static const Color secondary = Color(0xFF3949AB);

  /// 点缀色（Accent）：琥珀黄，用于按钮/高亮
  static const Color accent = Color(0xFFFFC107);

  /// 辅助色：蓝绿色，用于标签/图标
  static const Color tealish = Color(0xFF26A69A);

  /// 浅背景色：淡灰蓝背景
  static const Color lightBackground = Color(0xFFF5F7FB);

  /// 暗色背景（用于暗色模式可选）
  static const Color darkBackground = Color(0xFF0F172A);

  /// 字体主色（适用于深色背景）
  static const Color textOnDark = Colors.white;

  /// 字体主色（适用于浅色背景）
  static const Color textOnLight = Color(0xFF111827);

  /// 灰色文字（辅助信息）
  static const Color textHint = Color(0xFF9CA3AF);

  /// 白色
  static const Color white = Color(0xFFFFFFFF);

  /// 深灰
  static const Color dartGray = Color(0xFF4B5563);


}
